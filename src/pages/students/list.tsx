import { IResourceComponentsProps } from "@refinedev/core";
import {InferencerList} from './inferencer/list.inferencer'

export const StudentsList: React.FC<IResourceComponentsProps> = () => {
  return <InferencerList />;
};
